// 1
const sumNum = (a, b) => a + b
console.log(sumNum(2, 3))

// 2
const strLength = (str) => str.length

console.log(strLength('string'))

// 3
const incrementArrNum = (arr) => arr.map((num) => num + 1)

console.log(incrementArrNum([1, 2, 3, 4, 5]))
