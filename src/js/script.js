;(function () {
  const checkNumber = () => {
    const inputValue = parseInt(document.getElementById('input').value, 10)
    if (!Number.isNaN(inputValue) {
      const result = input % 2 === 0 ? 'Парне число' : 'Не парне число';
      alert(result);
    }
 	}
  }
})()
