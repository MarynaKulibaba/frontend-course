const addOption = () => {
  const optionInputs = document.getElementById('optionInputs')
  const optionCount = optionInputs.children.length + 1
}

document.querySelector('.add-btn-option').addEventListener('click', addOption)
